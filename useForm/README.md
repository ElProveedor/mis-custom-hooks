# useForm Hook

Ejemplo de uso:

```
    const initialform = {
        name: '',
        age: '',
        email: ''
    };

    const [ formValue, handleInputChange, reset ] = useForm(initialForm);

```
